




dependencies {
    compile(project(':wlxx-framework'))
    compile(project(':wlxx-user'))
}


// 解决多模块之间依赖，在build时找不到类的问题
sourceSets {
    main {
        java {
            srcDirs = ['src/main/java','../wlxx-framework/src/main/java','../wlxx-user/src/main/java']
        }
    }
}
