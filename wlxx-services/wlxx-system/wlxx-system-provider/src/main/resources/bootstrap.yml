spring: # Spring配置项
  application:
    name: wlxx-system
  #  profiles:
  #    active: public
  cloud: # SpringCloud配置项
    nacos: # Nacos注册中心的配置
      #        cluster-name: public # 配置集群名称
      config:
        server-addr: ${NACOS_ADDR:localhost:8848} # Nacos地址
        file-extension: yml # 配置文件的格式，默认为properties
        namespace: ${NAMESPACE:public}
        group: ${group-id:DEFAULT_GROUP}
        ext-config[0]:
          data-id: wlxx-mybatis.yml
          group: ${group-id:DEFAULT_GROUP}
          refresh: false
        ext-config[1]:
          data-id: wlxx-redis.yml
          group: ${group-id:DEFAULT_GROUP}
          refresh: false
        ext-config[2]:
          data-id: wlxx-security.yml
          group: ${group-id:DEFAULT_GROUP}
          refresh: false
        ext-config[3]:
          data-id: bean-searcher.yml
          group: ${group-id:DEFAULT_GROUP}
          refresh: false
        #        shared-configs:
        #          - data-id: wlxx-mybatis.yml
        #            group: ${group-id:DEFAULT_GROUP}
        #            refresh: false
        #          - data-id: wlxx-redis.yml
        #            group: ${group-id:DEFAULT_GROUP}
        #            refresh: false
        #          - data-id: wlxx-security.yml
        #            group: ${group-id:DEFAULT_GROUP}
        #            refresh: false
        #          - data-id: bean-searcher.yml
        #            group: ${group-id:DEFAULT_GROUP}
        #            refresh: false


